import MockAdapter from "axios-mock-adapter";

export default function registerMocks(mockInstance: MockAdapter) {
  // mockInstance.onGet("/users/1").reply(200, {
  //   user: { id: 1, name: "John Smith" },
  // })
}