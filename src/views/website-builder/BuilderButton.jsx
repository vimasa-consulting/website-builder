import styles from "../../styles/dashboard/websiteBuilder/builderHeader.module.scss";
const BuilderButton = () => {
  return <button className={styles.buttonCTA}>I am Button</button>;
};

export default BuilderButton;
